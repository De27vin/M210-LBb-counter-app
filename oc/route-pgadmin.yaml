apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: counter-pgadmin
spec:
  host: pgadmin.example.com  
  to:
    kind: Service
    name: counter-pgadmin   
  port:
    targetPort: 80  
  tls:
    termination: edge 
